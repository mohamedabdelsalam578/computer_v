# Data — too large for git (upload separately to HuggingFace/Drive)
data_raw/
lightning_logs/
# CLIP training outputs (checkpoints ~1GB, logs) — copy separately; do not commit
lightning_logs_clip/
results/
__pycache__/
*.pyc
.DS_Store
.cursor/
.idea/
*.egg-info/

# Weights — Resnet50 goes via Git LFS (104 MB)
# ferretnet-b-median-3.pth is small (4 MB) — tracked normally

# Repo copy of FerretNet (not needed)
_FerretNet_repo/
weights/Resnet50_Final.pth
